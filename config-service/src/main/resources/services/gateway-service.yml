hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 20000

ribbon:
  ReadTimeout: 20000
  ConnectTimeout: 20000

zuul:
  ignoredServices: '*'
  host:
    connect-timeout-millis: 20000
    socket-timeout-millis: 20000

  routes:
    user-service:
      path: /users/**
      serviceId: user-service
      stripPrefix: false

    payment-service:
      path: /payments/**
      serviceId: payment-service
      stripPrefix: false

#    deposit-service:
#      path: /auth/**
#      serviceId: authentification-service
#      stripPrefix: false
#    todo add authentification
server:
  port: 8989

